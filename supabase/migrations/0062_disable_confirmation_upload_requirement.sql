-- ============================================================
-- Органайзер — временно изключва изискването за прикачено
-- потвърждение преди завършване на задача (поискано изрично
-- 2026-08-25). Правилото от 0001_init.sql / buildCompletionUpdate
-- (index.html) остава в кода непокътнато — само флагът се сваля,
-- за да може лесно да се върне (UPDATE ... set true) без нова
-- миграция за логиката.
--
-- Засяга ВСИЧКИ активни (valid_to is null) типове задължения,
-- включително ДДС декларация и др. — не само Обр.1/Обр.6.
-- ============================================================

update obligation_types
set requires_confirmation_upload = false
where valid_to is null;
