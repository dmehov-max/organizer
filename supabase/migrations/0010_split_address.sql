-- ============================================================
-- Органайзер — адресът на управление се разделя на град + улица
-- (вместо едно свободно поле) — заявено веднага след 0007, преди
-- реално да е въведен адрес на жив клиент, затова просто заменяме
-- колоната, не мигрираме данни.
-- ============================================================

alter table clients
  drop column if exists address,
  add column address_city text,
  add column address_street text;
