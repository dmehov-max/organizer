-- ============================================================
-- Органайзер — номер на подадената декларация, задължителен при
-- отбелязване на "Подаване" в прогрес-таблицата (поискано изрично,
-- по образец на входящия номер, който вече се разпознава автоматично
-- от качени PDF потвърждения — но тук е ръчно поле, независимо от
-- наличие на прикачен файл).
-- ============================================================

alter table tasks
  add column submission_reference_number text;
