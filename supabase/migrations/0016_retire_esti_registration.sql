-- ============================================================
-- Органайзер — "Регистрация на обекта в ЕСТИ" отпада (виж 0014 за
-- същата логика — затваряме версията, не трием реда).
-- ============================================================

update obligation_types
set valid_to = current_date, active = false
where code = 'esti_registration' and valid_to is null;
