-- ============================================================
-- Органайзер — "Плащане" колоната/проследяването само за клиентите,
-- за които офисът реално плаща осигуровки/ДДС вместо тях (поискано
-- изрично 2026-08-26) — досега task_payments се създаваше за ВСЕКИ
-- клиент по задължение с requires_payment, независимо кой всъщност
-- превежда парите.
--
-- По подразбиране false за всички (включително съществуващите) —
-- админът трябва изрично да включи флага за всяка фирма, за която
-- офисът плаща (виж toggle в досието на клиента, index.html).
-- Съществуващите task_payments редове НЕ се трият тук — стара
-- история остава непокътната, просто новите задачи занапред няма да
-- родят нов ред за плащане, докато флагът не е включен.
-- ============================================================

alter table clients
  add column office_pays_obligations boolean not null default false;
