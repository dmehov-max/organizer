-- ============================================================
-- Органайзер — нов dossier flag: СОЛ с личен труд (самоосигуряващо
-- се лице, което получава и възнаграждение за личен труд от
-- дружеството — за това се подава Декл. обр. 6 ВСЕКИ МЕСЕЦ, за
-- разлика от обикновен СОЛ, който подава Обр.1/5 и Обр.6 само
-- годишно). Заявено изрично от Дойчин, 2026-08-09.
--
-- ВАЖНО: ALTER TYPE ... ADD VALUE не може да се ползва в същата
-- транзакция, в която стойността после се РЕФЕРЕНЦИРА (INSERT/DDL,
-- което я сравнява) — затова е в отделен файл/изпълнение, преди
-- 0009 (която вече вмъква ред в obligation_types с тази стойност).
-- ============================================================

alter type dossier_flag add value 'sol_personal_labor';
