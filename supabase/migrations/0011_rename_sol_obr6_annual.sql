-- ============================================================
-- Органайзер — козметично преименуване (не е промяна на срока, само
-- на етикета) — не се прави нова версия на реда, само UPDATE на
-- текущия (valid_to is null), защото версиониране пази историята
-- само при промяна на ПРАВИЛОТО (deadline_rule), не на текста.
-- ============================================================

update obligation_types
set name = 'Обр. 6 за СОЛ'
where code = 'sol_obr6_annual' and valid_to is null;
