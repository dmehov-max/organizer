-- ============================================================
-- Органайзер — "ГОД пред НСИ — консолидиран отчет" отпада (виж 0014
-- за същата логика — затваряме версията, не трием реда).
-- ============================================================

update obligation_types
set valid_to = current_date, active = false
where code = 'god_nsi_consolidated' and valid_to is null;
