-- ============================================================
-- Органайзер — козметично преименуване (виж 0011 за същата логика).
-- ============================================================

update obligation_types
set name = 'Болничен лист'
where code = 'sick_leave' and valid_to is null;
