-- ============================================================
-- Органайзер — "Авансови вноски ЗКПО — месечни/тримесечни" отпадат
-- (по изрична заявка — няма какво да им се следи в Органайзер).
--
-- Не трием реда (пази история/одит за вече създадени задачи по
-- него, ако има) — затваряме версията (valid_to = днес), точно
-- както е конвенцията за версиониране в 0001_init.sql. От утре тези
-- два obligation_types вече не се показват в чеклиста и
-- generate-tasks спира да им прави нови задачи (и двете четат само
-- valid_to is null).
-- ============================================================

update obligation_types
set valid_to = current_date, active = false
where code in ('zkpo_advance_monthly', 'zkpo_advance_quarterly') and valid_to is null;
